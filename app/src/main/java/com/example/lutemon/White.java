package com.example.lutemon;

public class White extends Lutemon{
    public White(String name, String color, int defence, int attack, int maxHealth) {
        super(name, color, defence, attack, maxHealth);
    }
}
